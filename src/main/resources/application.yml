server:
  port: 8088

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TicketingSystem
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
